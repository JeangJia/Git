#  Git 分支完全指南

> 分支是 Git 最强大的功能：让你在不影响主线代码的情况下，安全地开发新功能、修 Bug。
> 核心心法：**main 保持稳定，所有新工作都在自己的分支上做，做完再合并回 main。**

---

## 目录

1. [什么是分支（一分钟看懂）](#一什么是分支一分钟看懂)
2. [为什么单人开发也该用分支](#二为什么单人开发也该用分支)
3. [分支日常流程（标准用法）](#三分支日常流程标准用法)
4. [分支常用命令速查](#四分支常用命令速查)
5. [合并分支](#五合并分支)
6. [删除分支](#六删除分支)
7. [常见错误与解决](#七常见错误与解决)
8. [推荐工作流总结](#八推荐工作流总结)

---

## 一、什么是分支（一分钟看懂）

把仓库想象成一棵树：

- **main** 是树干（主线，永远放稳定可用的代码）
- **分支** 是从树干上长出的枝条（做实验、写新功能，随便折腾）

```text
          ●───●───●  feature/login（新功能分支）
         /
●───●───●───●─────●  main（主线，一直前进）
             ↑
        从这里长出分支
```

关键点：

- 在分支上的任何提交，**完全不影响 main**，搞砸了直接删掉分支即可
- 分支本质只是一个"可移动的指针"，指向某次提交，创建几乎零成本

---

## 二、为什么单人开发也该用分支

| 不用分支 | 用分支 |
| --- | --- |
| 写一半的代码不敢提交，怕 main 坏掉 | 随便提交，main 始终可用 |
| 想同时改两个功能，文件互相覆盖 | 每个功能一个分支，互不干扰 |
| 改错了想回退，牵一发动全身 | 分支不要了直接删，main 毫发无损 |
| 远程 main 是"稳定版"的说法无法成立 | main 永远是稳定版 |

**一句话：分支给你后悔药。**

---

## 三、分支日常流程（标准用法）

> 🚨 **黄金法则：开始新工作前，先切回 main 并拉取最新代码，再开新分支。**

```bash
# 1. 确保在最新的 main 上
git checkout main
git pull origin main

# 2. 基于最新 main 创建并切换到新分支（一步到位）
git checkout -b feature/login

# 3. 在分支上正常写代码，正常提交（和平时一样）
git add .
git commit -m "完成登录页面"
git add .
git commit -m "添加表单校验"

# 4. 功能写完了：推送分支到 GitHub
git push -u origin feature/login

# 5. 确认没问题后，合并回 main
git checkout main
git pull origin main
git merge feature/login

# 6. 推送合并后的 main
git push origin main

# 7. 分支用完即删（本地 + 远程）
git branch -d feature/login
git push origin --delete feature/login
```

### 分支命名习惯

```text
feature/xxx   新功能，如 feature/login
fix/xxx       修 Bug，如 fix/404-page
test/xxx      试验性代码，如 test/new-layout
```

> 💡 新版 Git 可以用 `git switch -c 分支名` 代替 `git checkout -b`，语义更清晰（见下文）。

---

## 四、分支常用命令速查

```bash
# 查看所有本地分支（当前分支前有 * 号）
git branch

# 查看所有分支（含远程分支）
git branch -a

# 创建新分支（不切换过去）
git branch feature/login

# 切换分支
git checkout feature/login
# 等价新写法（推荐）：
git switch feature/login

# 创建 + 切换，一步到位（最常用）
git checkout -b feature/login
# 等价新写法：
git switch -c feature/login

# 查看每个分支的最后一次提交
git branch -v

# 重命名当前分支
git branch -m 新名字
```

> ⚠️ 切换分支前，先 `git status` 确认工作区干净（没有未提交的修改），否则修改会"跟着你"跑到另一个分支。

---

## 五、合并分支

### 5.1 普通合并（fast-forward，最常见）

```bash
# 把 feature/login 合并进 main
git checkout main
git merge feature/login
```

如果 main 在你开发期间没有新提交，Git 会直接"快进"，历史是一条直线，干干净净。

### 5.2 产生合并提交（三方合并）

如果 main 和分支都有各自的提交，合并会产生一个"合并提交"：

```text
  ●───●  feature/login
 /     \
●───●───●  main（多了个合并提交）
```

```bash
# 想让历史保持一条直线，用 rebase 方式合并（推荐单人使用）
git checkout main
git merge --no-ff feature/login   # 保留分支拓扑
# 或者：
git rebase feature/login          # 变基，历史线性
```

### 5.3 合并时遇到冲突

```bash
git merge feature/login
# 提示 CONFLICT，打开冲突文件，找到标记：
# <<<<<<< HEAD
# 当前分支的代码
# =======
# 进来的分支的代码
# >>>>>>> feature/login

# 手动删掉标记、保留正确代码后：
git add .
git commit -m "解决合并冲突"
```

> 💡 冲突不可怕，只是 Git 让你人工决定"留哪边"。改完 `add` + `commit` 就结束了。

### 5.4 合并前先看差异（好习惯）

```bash
# 看分支相对 main 改了什么
git diff main..feature/login

# 看分支上多了哪些提交
git log main..feature/login --oneline
```

---

## 六、删除分支

```bash
# 删除已合并的本地分支（安全）
git branch -d feature/login

# 强制删除（未合并也删，慎重）
git branch -D feature/login

# 删除远程分支
git push origin --delete feature/login
```

> 💡 分支合并完就删，保持分支列表干净。需要时随时可以从 main 再开。

---

## 七、常见错误与解决

### 错误 1：切换分支时带着未提交的修改

```bash
git stash            # 先把修改暂存起来
git checkout main    # 安心切换
git checkout feature/login
git stash pop        # 回来后恢复修改
```

### 错误 2：在 main 上写了几行代码，才发现应该在分支上写

```bash
# 修改还没 add / commit 时，直接创建分支，修改会自动跟过来
git checkout -b feature/login
git add .
git commit -m "..."
```

如果已经 commit 到 main 了，把这一个提交"搬"到新分支：

```bash
git branch feature/login     # 在当前提交建分支
git reset --hard HEAD~1      # main 回退一步（撤销提交，⚠️ 未提交修改会丢）
git checkout feature/login  # 切到分支，提交都在
```

### 错误 3：合并后想反悔

```bash
# 撤销最近一次合并（还没 push 时）
git reset --hard HEAD~1
```

### 错误 4：不小心在 main 上直接 push 了半成品

```bash
# 回到上一个稳定提交并覆盖远程（⚠️ 会丢提交，慎用）
git reset --hard HEAD~1
git push origin main --force
```

### 错误 5：远程有别人的分支更新，本地看不到

```bash
git fetch origin       # 同步远程分支信息
git branch -a          # 现在能看到了
```

---

## 八、推荐工作流总结

**单人 / 多设备开发，推荐这样用分支：**

```bash
# 每次开始新工作：
git checkout main && git pull origin main
git checkout -b feature/xxx

# ...写代码、提交、推送...
git push -u origin feature/xxx

# 完成、验证没问题后：
git checkout main
git pull origin main
git merge feature/xxx
git push origin main
git branch -d feature/xxx
```

**main 只放稳定代码 → 新工作开分支 → 做完合并回来 → 分支用完就删。**

配合 [每天都用.md](./每天都用.md) 的日常提交习惯使用，完整流程见 [SSH提交流程.md](./SSH提交流程.md)。
